RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME} !-f

# Should leave /pages/test
RewriteRule ^(.*)$ $1.html [NC, L]

#Should leave /test
RewriteRule ^(.*)$ /pages/$1 [NC, L]
